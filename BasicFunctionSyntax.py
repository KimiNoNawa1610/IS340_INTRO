
def main(): #function header
    x=14 #local variable (only exist inside the function)
    if 0<=x<100:
        return True #evaluated to true
    else:
        return False #evaluated to false
main() #calling the function
